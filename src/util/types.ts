export type TForm ={
    nome: string,
    img: string
};

export type TTema={
    tema: {
        tema: string;
    };
}

export type TNome = {
    nome: {
        nome: string,
        img: string
    };
}